export interface Grid {
        roomLabel: string;
        width: string | number;
        height: string | number;
        configuration: string;
        left: string;
        right: string;
        activePanel: string;
}
